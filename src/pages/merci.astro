---
import Layout from "@/layouts/layout.astro";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter } from "@/components/ui/card";
import { CheckCircle2, Mail, ArrowLeft } from "lucide-react";
---

<Layout>
  <main class="min-h-screen bottom-24 flex items-center justify-center p-4 relative overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-[500px] bg-linear-to-b from-primary/5 to-transparent -z-10 pointer-events-none"></div>
    
    <Card className="max-w-lg w-full text-center border-none shadow-xl bg-card/50 backdrop-blur-sm">
      <CardHeader className="flex flex-col items-center gap-4 pb-2">
        <div class="p-4 bg-primary/10 rounded-full text-primary ring-8 ring-primary/5">
          <CheckCircle2 className="w-12 h-12" />
        </div>
        <CardTitle className="text-3xl font-bold">Message reçu !</CardTitle>
        <CardDescription className="text-lg">
          Merci de nous avoir contactés.
        </CardDescription>
      </CardHeader>
      
      <CardContent className="space-y-6 pt-4">
        <p class="text-muted-foreground leading-relaxed">
          Votre demande de devis a bien été enregistrée. Nous allons étudier votre demande avec attention et vous répondre sous <span class="font-semibold text-foreground">48 heures</span>.
        </p>
        
        <div class="bg-muted/50 p-4 rounded-lg text-sm text-muted-foreground border border-border/50">
          <p class="flex flex-col gap-1 items-center justify-center">
            <span>Passé ce délai, si vous n'avez pas de nouvelles, contactez-nous directement à :</span>
            <a href="mailto:contact@portalstudio.fr" class="flex items-center gap-2 font-medium text-primary hover:underline mt-1">
              <Mail className="w-4 h-4" />
              contact@portalstudio.fr
            </a>
          </p>
        </div>
      </CardContent>

      <CardFooter className="justify-center pt-2 pb-8">
        <a href="/">
          <Button className="gap-2" size="lg">
            <ArrowLeft className="w-4 h-4" />
            Retour à l'accueil
          </Button>
        </a>
      </CardFooter>
    </Card>
  </main>
</Layout>
